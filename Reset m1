local vu1 = game:GetService("Players").LocalPlayer
local vu2 = game:GetService("UserInputService")
local vu3 = game:GetService("TweenService")
local vu4 = game:GetService("RunService")
local v5 = game:GetService("Workspace")

local vu6 = nil
pcall(function()
    vu6 = game:GetService("VirtualInputManager")
end)
if not vu6 then
    local v7 = rawget(_G, "VirtualInputManager")
    if v7 then vu6 = v7
    else
        local v8 = rawget(_G, "syn") and rawget(_G, "syn").virtual_input
        vu6 = v8 or rawget(_G, "virtualinputmanager")
    end
end

local vu45 = false
local vu46 = {}
local vu47 = v5.CurrentCamera

local function vu50()
    local v48 = vu1 and vu1.Character
    local v49 = v48 and v48:FindFirstChildOfClass("Humanoid")
    return v48, v49
end

local function vu59()
    local _, v51 = vu50()
    local vu52 = v51 and v51:FindFirstChildOfClass("Animator")
    if vu52 then
        local v53, v54 = pcall(function() return vu52:GetPlayingAnimationTracks() end)
        if v53 and v54 then
            for _, track in ipairs(v54) do
                pcall(function() track:Stop() end)
            end
        end
    end
end

local function vu66(p60)
    local _, v61 = vu50()
    if v61 then
        local vu62 = v61:FindFirstChildOfClass("Animator") or Instance.new("Animator", v61)
        local vu63 = vu46[p60]
        if not vu63 then
            vu63 = Instance.new("Animation")
            vu63.AnimationId = "rbxassetid://" .. tostring(p60)
            vu46[p60] = vu63
        end
        local v64, vu65 = pcall(function() return vu62:LoadAnimation(vu63) end)
        if v64 and vu65 then
            vu65.Priority = Enum.AnimationPriority.Action
            vu65:Play()
            pcall(function() vu65:AdjustSpeed(1.2) end)
            return vu65
        end
    end
end

local function vu75(pu67)
    if not vu45 then
        vu45 = true
        local vu68, _ = vu50()
        vu68 = vu68 and vu68:FindFirstChild("HumanoidRootPart")
        if vu68 then
            pcall(function()
                vu68.CFrame = vu68.CFrame * CFrame.Angles(0, math.rad(pu67), 0)
                local v69 = vu47 and vu47.CFrame.Position or vu68.Position + Vector3.new(0, 5, 0)
                local v70 = v69.Y
                local v71 = v69 - vu68.Position
                local v72 = Vector3.new(v71.X, 0, v71.Z)
                local v73 = CFrame.fromAxisAngle(Vector3.yAxis, math.rad(pu67)) * v72
                local v74 = vu68.Position + v73
                if vu47 and vu47:IsA("Camera") then
                    vu47.CFrame = CFrame.lookAt(Vector3.new(v74.X, v70, v74.Z), vu68.Position)
                end
            end)
        end
        vu45 = false
    end
end

local function vu81(p76, p77, p78)
    if not vu45 then
        vu45 = true
        local v79, _ = vu50()
        v79 = v79 and v79:FindFirstChild("HumanoidRootPart")
        if v79 then
            local vu80 = vu3:Create(v79, TweenInfo.new(p78, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
                CFrame = v79.CFrame * CFrame.new(p76 * p77, 0, 0)
            })
            pcall(function() vu80:Play() end)
            if vu80 then pcall(function() vu80.Completed:Wait() end) end
            pcall(function() vu80:Destroy() end)
        end
        vu45 = false
    end
end

local function doM1Reset()
    vu59()
    pcall(function() vu66(10480793962) end)
    pcall(function() vu81(1, 26, 0.22) end)
    pcall(function() vu75(70) end)
    task.wait(0.003)
    pcall(function()
        if vu6 and type(vu6.SendKeyEvent) == "function" then
            vu6:SendKeyEvent(true, Enum.KeyCode.Q, false, game)
            vu6:SendKeyEvent(false, Enum.KeyCode.Q, false, game)
        end
    end)
end

local oldUi = game:GetService("CoreGui"):FindFirstChild("M1ResetMobileUI") or vu1:WaitForChild("PlayerGui"):FindFirstChild("M1ResetMobileUI")
if oldUi then oldUi:Destroy() end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "M1ResetMobileUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global

local synUi = rawget(_G, "syn")
if synUi and type(synUi.protect_gui) == "function" then
    pcall(function() synUi.protect_gui(screenGui) end)
end

local parentGui = pcall(function() return game:GetService("CoreGui") end) and game:GetService("CoreGui") or vu1:WaitForChild("PlayerGui")
screenGui.Parent = parentGui

local mobileBtn = Instance.new("TextButton")
mobileBtn.Name = "M1ResetButton"
mobileBtn.Size = UDim2.fromOffset(70, 70)
mobileBtn.Position = UDim2.new(0.75, 0, 0.5, 0)
mobileBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mobileBtn.TextColor3 = Color3.fromRGB(0, 215, 255)
mobileBtn.Text = "M1"
mobileBtn.Font = Enum.Font.GothamBlack
mobileBtn.TextSize = 20
mobileBtn.Parent = screenGui

local uiCorner = Instance.new("UICorner", mobileBtn)
uiCorner.CornerRadius = UDim.new(1, 0)

local uiStroke = Instance.new("UIStroke", mobileBtn)
uiStroke.Color = Color3.fromRGB(0, 215, 255)
uiStroke.Thickness = 2
uiStroke.Transparency = 0.3

local dragging = false
local dragInput, dragStart, startPos

mobileBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mobileBtn.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

mobileBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

vu4.RenderStepped:Connect(function()
    if dragging and dragInput and dragStart and startPos then
        local delta = dragInput.Position - dragStart
        mobileBtn.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X, 
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

local function playClickEffect()
    mobileBtn.Size = UDim2.fromOffset(65, 65)
    task.spawn(doM1Reset)
    task.wait(0.1)
    mobileBtn.Size = UDim2.fromOffset(70, 70)
end

mobileBtn.MouseButton1Click:Connect(playClickEffect)

vu2.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.R then
        task.spawn(doM1Reset)
        
        task.spawn(function()
            mobileBtn.Size = UDim2.fromOffset(65, 65)
            task.wait(0.1)
            mobileBtn.Size = UDim2.fromOffset(70, 70)
        end)
    end
end)

print("M1 Reset Loaded!")
